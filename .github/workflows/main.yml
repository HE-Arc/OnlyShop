name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "master" branch
  push:
    branches: ["master", "lucas_dev_2"]
  pull_request:
    branches: ["master"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  doc-job:
    runs-on: ubuntu-latest #specify runner
    container:
      image: ghcr.io/perrin-lucas/docker-image-apidoc:v1.0
    steps:
      - name: Checkout
        uses: actions/checkout@v2 # This action checks-out your repository under $GITHUB_WORKSPACE, so your workflow can access it. --> see https://github.com/actions/checkout

      - name: Generate documentation
        run: |
          ls -al
          apidoc -i ./app/Http/Controllers -o .

      - name: artifact
        uses: actions/upload-artifact@v3
        with:
          name: api-docs
          path: index.html
